<?xml version="1.0"?>
<project name="Lab3" default="compile">

    <property name="srcDir" location="src/java/yahoo/eu"/>
    <property name="testsSrcDir" location="src/test"/>
    <property name="buildDir" location="build"/>
    <property name="compiledClassesDir" location="${buildDir}/classes"/>
    <property name="mainClass" value="yahoo.eu.Main"/>
    <property name="scp.host" value="77.234.214.82"/>
    <property name="scp.username" value="s284710"/>
    <property name="scp.password" value="ltw411"/>
    <property name="scp.port" value="2222"/>
    <property name="scp.destdir" value="/home/s284710"/>
    <property name="report.dir" location="report"/>

    <target name="compile">
        <mkdir dir="${buildDir}/classes"/>
        <javac destdir="${compiledClassesDir}"
               includeantruntime="false"
               classpath="lib/junit-4.12.jar">
            <src location="${srcDir}"/>
            <src location="${testsSrcDir}"/>
        </javac>
    </target>

    <target name="build" depends="compile">
        <jar destfile="build/${ant.project.name}.jar"
             basedir="${compiledClassesDir}"
             excludes="Test*.class">
            <manifest>
                <attribute name="Main-Class"
                           value="yahoo.eu.Main"/>
            </manifest>
        </jar>
    </target>

    <target name="clean">
        <delete dir="${buildDir}"/>
    </target>

    <target name="run" depends="build">
        <java jar="${buildDir}/${ant.project.name}.jar" fork="true"/>
    </target>

    <target name="test" depends="compile">
        <delete dir="${report.dir}"/>
        <mkdir dir="${report.dir}"/>
        <mkdir dir="${report.dir}/raw"/>
        <junit printsummary="yes" showoutput="yes">
            <classpath location="${compiledClassesDir}"/>
            <classpath location="lib/junit-4.12.jar"/>
            <classpath location="lib/hamcrest-core-1.3.jar"/>
            <test name="TestMain" todir="${report.dir}/raw"/>
            <formatter type="xml"/>
        </junit>
    </target>

    <target name="report" depends="test">
        <junitreport todir="${report.dir}">
            <fileset dir="${report.dir}/raw/">
                <include name="TEST-*.xml" />
            </fileset>
            <report format="noframes" todir="reports/html/" />
        </junitreport>
    </target>

    <target name="scp" depends="build">
        <scp file="${buildDir}/${ant.project.name}.jar"
             todir="${scp.username}:${scp.password}@${scp.host}:${scp.destdir}"
             port="${scp.port}"
             trust="true"/>
    </target>

    <target name="alt">

    </target>

    <target name="doc">
        
    </target>
</project>